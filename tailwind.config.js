const colors = require("tailwindcss/colors");

module.exports = {
  theme: {
    colors: {
      ...colors,
      white: "#FFFFFF",
      black: "000000",
      active: "36CE00",
    },
  },
  plugins: [],
};
